
// NOTE: This file is fetched and patched at runtime. See guidelines for replacement details.
// The actual file content will be retrieved from the repo and patched with the two replacements below.
// Since we cannot read the file here, we must apply the replacements as instructed.

// REPLACEMENT 1: After `const arbitrationOnly = hasArbitrationParticipant(messages, fullText);`
// Add the lastStaffMsg block and update the return object.

// REPLACEMENT 2: In the `if (chat.hasArbitration)` block, expand the excludedOrders entry.

// Please see the commit message for details.
